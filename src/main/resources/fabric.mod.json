{
  "schemaVersion": 1,
  "id": "${mod_id}",
  "version": "${version}",
  "name": "${mod_name}",
  "description": "${description}",
  "authors": [
    "${mod_author}\n\n"
  ],
  "contact": {
    "homepage": "https://modrinth.com/mod/${modrinth_slug}",
    "sources": "https://github.com/${github_repo}"
  },
  "license": "${license}",
  "icon": "${mod_id}.png",
  "environment": "client",
  "entrypoints": {
    "main": [
      "sylenthuntress.sharper_tooltips.SharperTooltips"
    ]
  },
  "mixins": [
    "${mod_id}.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=${fabric_loader_version}",
    "minecraft": ">=1.21 <=1.21.5",
    "java": ">=${java_version}"
  },
  "custom": {
    "modmenu": {
      "links": {
        "sylenthuntress.discord": "${discord_invite}"
      },
      "update_checker": true
    }
  }
}
