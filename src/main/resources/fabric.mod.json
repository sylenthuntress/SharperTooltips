{
  "schemaVersion": 1,
  "id": "${mod_id}",
  "version": "v${version}+${minecraft_version}+Fabric",
  "name": "${mod_name}",
  "description": "${description}",
  "authors": [
    "${mod_author}\n\n"
  ],
  "contact": {
    "homepage": "https://modrinth.com/mod/${modrinth_slug}",
    "sources": "https://github.com/${github_repo}"
  },
  "license": "${license}",
  "icon": "${mod_id}.png",
  "environment": "*",
  "entrypoints": {
    "main": [
      "sylenthuntress.examplemod.ExampleMod"
    ]
  },
  "mixins": [
    "${mod_id}.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=${fabric_loader_version}",
    "fabric-api": ">=${fabric_version}",
    "minecraft": "${minecraft_version}",
    "java": ">=${java_version}"
  },
  "custom": {
    "modmenu": {
      "links": {
        "examplemod.discord": "${discord_invite}"
      },
      "update_checker": true
    }
  }
}
